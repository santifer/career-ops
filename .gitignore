# Personal data (user fills these)
cv.md
data/applications.md
data/pipeline.md
data/scan-history.tsv
data/vimeo-inventory-*.md
data/heartbeat-*.md
data/heartbeat-archive/*.html
.claude/knowledge/brain/personality-*.md
data/triage-batch.tsv
data/curator-mentions.md
data/rejection-history.*
data/grok-spend.log
data/ingestion-report-*.md
data/dry-run-state.md
data/apply-workflow.md
data/return-day-checklist.md
data/alerts-signup-checklist.md
data/logs/
data/pending-diffs/*.md
!data/pending-diffs/.gitkeep
data/research-state.json
# Gmail-poll history state (P0-3) — persists lastHistoryId + rolling
# message-id cache for scripts/scan-email-poll.mjs. Regenerated on each
# poll; safe to delete (triggers a bootstrap on next run).
data/gmail-history-state.json
# Health column coverage report (derived from applications.md — personal pipeline state)
data/health-column-coverage.json
# Role-enrichment validator telemetry (per-write JSONL log, soft-fail observation
# window for symmetric gating wired 2026-05-19). Regenerated on each enrich-apply-now
# run; references row numbers + companies from personal pipeline state.
data/role-enrichment-validator-telemetry.jsonl
# Update Drawer (B5 MVP, 2026-05-18) — Mitchell's career updates feed.
# Append-only JSONL is personal data; cursor tracks merge progress for
# scripts/merge-career-updates.mjs idempotency. Never commit either.
data/career-updates.jsonl
data/career-updates-merged-cursor.txt
# Hiring-manager intel (7-LLM council output, contains contact info + outreach hooks)
data/hm-intel/*.json
!data/hm-intel/_SCHEMA.md
!data/hm-intel/_weights.json
# Side-allocation registry (ingest-feature-strategy finding #22 — always committed)
!data/side-allocations.yml
reports/*.md
reports/**/*.md
!reports/.gitkeep

# Generated dashboard HTML (contains user job-search data)
dashboard/index.html
output/*
!output/.gitkeep
batch/logs/*
!batch/logs/.gitkeep
batch/batch-state.tsv
batch/batch-state.archive-*.tsv
batch/batch-input.tsv
batch/batch-input.original-*.tsv
batch/tracker-additions/**/*.tsv
!batch/tracker-additions/.gitkeep
jds/*
!jds/.gitkeep
corpus/

# Auto-generated per-role apply-packs (personal output)
apply-pack/
data/apply-packs/

# Generated dashboard reports (personal)
dashboard/reports/

# Backup of user-specific portals.yml
portals.yml.santifer-backup
portals.yml.*-backup

# Writing samples (user's personal files — never committed)
writing-samples/*
!writing-samples/README.md

# User config and customization (never auto-updated)
config/profile.yml
portals.yml
modes/_profile.md
.update-dismissed
.update-lock

# Secrets (never commit — use .env.example as template)
.env

# Generated
.resolved-prompt-*
node_modules/
bun.lock
data/strategy-cache/

# OS
.DS_Store
*.mov
*.mp4

# Claude Code local
.claude/settings.local.json
.claude/memory/
career-dashboard
package-lock.json
article-digest.md

# Weekly intel output (user-specific, date-stamped)
data/weekly-intel/*.md
data/weekly-intel/prompts/

# Weekly progress tracker output + MCP enrichment templates (personal extracted intel)
data/weekly-progress/
data/weekly-progress-tasks/

# 1:1 career history (personal meeting notes)
data/career-history/

# Prebaked context bundle (generated, not source)
data/baked-context.md
data/baked-context.hash

# Cost tracking and URL dedup cache (runtime-generated, contains user pipeline data)
data/cost-log.tsv
data/triage-cache.tsv
data/url-resolve-cache.tsv

# Share-link tokens (runtime-generated, recruiter-facing read-only links)
data/share-tokens.json

# Per-row notes & activity log (personal data, never commit)
data/row-notes.json
# Lighthouse CI artifacts (per-run reports, ephemeral)
.lighthouseci/

# LinkedIn export — personal contact data, never commit
data/linkedin/

# Outreach state — per-contact message history + intel cache, personal
data/outreach-state.json

# VC portfolio scanner state — last_seen_created_at per firm; per-run trace
data/vc-portfolios-state.json

# Outreach drafts — recipient-facing content not yet sent; personal
data/outreach-drafts/

# Discard reasons — feeds back into triage prompt; personal data
data/discard-reasons.jsonl

# Composite toxicity score cache + Mitchell's tradeoff overrides (inventory
# item #4, 2026-05-18). Cache regenerates on every dashboard build; overrides
# are personal decisions — never commit either.
data/toxicity-composite.json
data/toxicity-overrides.jsonl

# Evaluation-completeness audit output (scripts/audit-all-evaluations.mjs);
# lists personal applications + scores. Re-generated on demand.
data/audit-evaluations-*.md

# Company intel cache — per-company council intel + toxicity + TTO, personal
data/company-intel-cache/

# Contact discovery output — per-company unified contact cards (1st/2nd-degree
# LinkedIn + Gmail + Drive + Calendar surfaces). Contains contact info; never commit.
data/contact-cards/
data/contact-discovery-tasks/

# Per-run council intel orchestrator artifacts (re-generated every run; would
# accumulate fast). Cost summary lives in the persisted data/cost-log.tsv;
# per-company intel lives in data/company-intel-cache/ (also gitignored).
data/process-all-council-intel-*-summary.md
data/process-all-council-intel-*.log

# Weekly progress tracker output — personal extracted intel from Gmail/Drive/Calendar/git.
data/weekly-progress/
data/weekly-progress-tasks/

# LLM usage knowledge base — local scanner cache, contains Mitchell's filesystem footprint
data/llm-usage-knowledge.json

# Network graph — private contact data, never commit
data/network-graph.json
# Network database (ZETA 2026-05-19) — aggregated personal-CRM index with
# emails + confidence bands + verification timestamps. Never commit.
data/network-database.json
data/network-database-saved-searches.json
data/network-database-cache/
data/zeta-post-impl-snapshots/
# Z-AAA fix 2026-05-19 — overlay files written by /api/network/person/:id/notes
# (notes) and by network-enricher + network-emailer (enrichments). Both contain
# personal data and must never reach the remote.
data/network-database-notes.json
data/network-database-enrichments.json
data/heartbeat-cache/

# AI detection gate cache — per-text SHA1 results, regenerable, 30-day TTL
data/ai-detection-cache/

# AI detection sidecar results (written next to each artifact)
**/*.ai-detection.json

# Contacts month-1 launch (2026-05-19, Phase A.8) — relationship-intelligence
# layer. Photos are scraped from LinkedIn auth'd sessions; enrichment cache
# carries LLM-synthesized outreach positioning + engagement topics; notes are
# Mitchell's per-contact annotations; second-brain index is a local file scan
# that fingerprints content for popover-citation lookups. All personal.
data/contact-photos/
data/contact-enrichment-cache/
data/contact-notes/
data/second-brain-*.md
data/second-brain-extracted/
data/contact-photo-queue.jsonl
data/contact-photos-failed.md
data/linkedin-storage-state.json
data/contact-enrichment-paused.md
data/contact-enrichment-month-1-audit.md
data/contact-enrichment-weights.diff
data/builder-evolution-popovers/

# launchd-wrapper (P1-8) runtime state — per-host rolling history, regenerated
# on every run; would be a constant-diff committable mess.
data/launchd-wrapper-state.json
data/launchd-wrapper-state.json.tmp
