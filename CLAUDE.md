@AGENTS.md

## Claude Code Notes

- This file intentionally stays a thin wrapper around `AGENTS.md`.
- Put shared career-ops behavior in `AGENTS.md`; keep only Claude Code-specific notes here.
- Claude Code slash-command entrypoints are materialized from `.agents/skills/career-ops/SKILL.md`.
