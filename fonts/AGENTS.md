# Fonts Context

`fonts/` is system layer. It stores self-hosted fonts used by CV templates and generated PDFs.

Do not replace fonts casually. Font changes can alter PDF pagination, ATS readability, and visual identity.

If changing fonts, check `templates/cv-template.html` and regenerate at least one PDF to verify layout.
