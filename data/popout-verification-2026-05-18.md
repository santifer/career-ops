# Popout / Drill-In / Child-Page Verification — 2026-05-18

Pure investigation against three inventories (A: 9-section spec + popouts, B: calibration interview asks, C: 22-item session transcript).

## Master Verification Table

| Item | Inv. | Status | Evidence | Gap / Notes | Effort |
|---|---|---|---|---|---|
| A1. Discard-with-reason popup → JSONL → next eval consumes | A | ✅ verified | `dashboard-server.mjs:2959-2995` (endpoint), `scripts/build-dashboard.mjs:11525` (POST), `lib/discard-pattern-injector.mjs:6,35`, `triage.mjs:38,309`, `batch-runner-batches.mjs:35,460` (downstream consumption) | Full loop closed — `window.prompt()` → `/api/discard-with-reason` → `data/discard-reasons.jsonl` → triage + batch prompt injection + heartbeat surfacing | — |
| A2. Live every-second cross-surface refresh on discard | A | ⏳ partial (event-driven only; no 1s poll) | `scripts/build-dashboard.mjs:16693,18483,22754` (`careerops:status-changed` dispatch + listeners); `scripts/build-dashboard.mjs:22761` (outreach polls every 60s) | Cross-surface propagation IS event-driven (instant on status change, ~200ms server-write delay). NO 1-second poller. Outreach pulse + apply-now badges re-fetch on event but heartbeat email is static (rebuilds on its own schedule). | small (~30 min) if a 1s poll is wanted, but event-driven is arguably better |
| A3. Apply button "direct to apply page" (ATS deep link) | A | ❌ not built | `scripts/build-dashboard.mjs:2597-2598` — just opens `url` (the JD URL) in a new tab; `lib/resolve-ats-url.mjs:195` (regex only used for in-page extraction, not for apply button) | Currently Apply button = JD URL. No deep-apply-form resolution. Low value defer per spec. | medium (~2h) — would need ATS detection + form-URL resolver |
| A4. Sidebar nav → additional popout queues / child pages | A | ⏳ partial | `scripts/build-dashboard.mjs:9302-9323` — sidebar has 5 nav targets: Overview, Apply-Now, All Evaluations, Trends, Companies. No "Pipeline Pending" or "Batch Runs" page. | Pipeline pending currently lives at `data/pipeline.md` (not surfaced in sidebar); batch runs live in `#mc-batch` modal at top strip. | medium (~2h) — add 2 sidebar targets + 2 anchor sections |
| A5. `#pipeline-modal` Process All + Run Batch popups | A | ✅ verified | `scripts/build-dashboard.mjs:6862-6939` (modal CSS), `9347-9348` (HTML), `pipeline-modal-wide` class for Process All | Cost estimate, budget panel, send-email checkbox all confirmed shipped | — |
| A6. `#pipeline-toast` in-flight progress popup | A | ✅ verified | `scripts/build-dashboard.mjs:7141-7160` (CSS class + bar) | Shipped | — |
| A7. Outreach Pulse banner + expand (`.op-banner`, `.op-expanded`, localStorage) | A | ✅ verified | `scripts/build-dashboard.mjs:21379-21412` (banner CSS), `20254-20261` (expand state), `22754` (cross-surface reactive) | Shipped | — |
| A8. All chip popovers (status, people, comp, equity, location, benefits, tier tag, gap modal, verify button, email button) | A | ✅ verified | Status: `2893`; People: `1066,1089`; Comp drill: `2639`; Equity: `686,699`; Location: `981,1010`; Benefits: `1020,1048`; Tier: `2891`; Gap: `2764,7214,11099`; Verify: `2604-2606`; Email: `2607` | All chips emit `pill-popover-trigger`/drill attrs and have keyboard + click handlers | — |
| A9. Report link (.html), Apply button, drawer Apply button | A | ✅ verified | Apply: `2597`; Report: `2600-2601`; Drawer Apply: `11483-11484` (`data-drawer-action="apply"` w/ data-drill) | — | — |
| **B1. Wealth-Ranking Pop-Out** | B | 🚫 hard exclusion | (new analytical surface, requires wealth-trajectory model) | — | — |
| **B2. Industry Gap Radar** | B | 🚫 hard exclusion | (new product, needs compensation surveys pipeline) | — | — |
| B3. Series-C+ Trajectory drill-down | B | ⏳ partial (lib exists, not surfaced in drawer) | `lib/wealth-lens.mjs:123,243` (exports `applyWealthLens` + `renderWealthLensCard`); `scripts/build-dashboard.mjs:32` IMPORTS them; `grep -c applyWealthLens scripts/build-dashboard.mjs` = **1** (only the import line — never called). Series-C signal IS present in the company drill-in IPO/Funding section via `companyFunding[slug]` (`scripts/build-dashboard.mjs:12154-12164`) but not via wealth-lens. | Wealth-lens is library-only; never rendered to any drawer. Company IPO/Funding card exists but only renders when `data/company-funding/{slug}.json` is populated. | medium (~2h) — wire `renderWealthLensCard` into comp drill-in and/or company drill-in section |
| **B4. Toxicity Composite Score** | B | 🚫 hard exclusion | — | (Note: drill-in section EXISTS — `scripts/build-dashboard.mjs:12099-12115` — but pulls from `data/toxicity/{slug}.json` which is mostly empty; full aggregation pipeline is the excluded part) | — |
| **B5. Update Drawer** | B | 🚫 hard exclusion | — | — | — |
| **B6. Weekly Gemini Calibration Prompt** | B | 🚫 hard exclusion | — | — | — |
| B7. Comp-elasticated Travel Tradeoff | B | ❌ not built | `grep -rln "travel" lib/ modes/ --include="*.md" --include="*.mjs"` returned empty. No travel-cap or travel-elasticity check anywhere. | New scoring dimension would need to land in `modes/oferta.md` + a `lib/travel-cap.mjs` lookup, with comp delta math. | medium (~2-3h) |
| C1. Scan banner role click → role popout | C | ✅ verified | `scripts/build-dashboard.mjs:10171-10182` (`.ticker-drill-company` + click handler → `drillIn('company',slug,e)`); `10244` (`.ticker-drill-role` → `drillIn('role',rowId,e)`) | Both company and role tokens in the live-ticker are drill-clickable | — |
| C2. Comp chip → comp breakdown + peer-company comparison | C | ⏳ partial (renders peer table only when baked data exists) | `scripts/build-dashboard.mjs:2639` (chip emits `drill-trigger`); `12205-12270` (drill-in renderer includes peer table + equity sliders + negotiation playbook for base ≥ $300K); `lib/peer-context.mjs:1-184` (pipeline-relative percentile + peer table generator) | Quality: `peerComp` data is keyed by drill-in id and only populated for select rows; many rows render the "Peer comp data not available" placeholder. Peer table is internal-pipeline-relative (not levels.fyi). | small (≤30 min) — bake `peerComp` for ALL rows with comp at build time |
| C3. Percentage pills → rationale + strategy child pages | C | ⏳ partial | `scripts/build-dashboard.mjs:2666` (pct emits `drill-trigger`); `12364` (percentage drill renderer); `lib/strategy-ceiling.mjs` (depth via `data/strategy-ceiling`) | Renderer exists; depth-of-content varies per metric. | small (≤30 min) for content QA pass |
| C4. Footnotes → PDF child pages via Acrobat | C | ⏳ partial (chain assembled, not auto-rendered for stories) | `lib/story-child-page.mjs:335-358,544-571` (footnotes pushed into story HTML); `lib/pdf-child-page.mjs:31,110` (wraps for PDF flavor); `dashboard-server.mjs:4369` (`/draft/{rowId}` renders child page) | Story child pages exist in `dashboard/stories/` (>20 files). No dedicated PDF button on the story page — would need a "Download as PDF" link calling `lib/pdf-child-page.mjs`. | small (≤30 min) — surface "Download PDF" link on each story page |
| C5. Universal drill-in registry — `data-drill` on every element | C | ✅ verified | `scripts/build-dashboard.mjs:11891,12009` (`_drillInRegister`); 17 types registered: role, company, status, score, comp, gap, story, metric, banner-roles, percentage, ingest-form, tpgm-gaps, readiness, time-to-offer, network-leverage, pack-stage-result, drawer-action, allocation | All 17 types confirmed. | — |
| C6. Peer-context lens on every chip | C | ⏳ partial | `scripts/build-dashboard.mjs:30` imports `getPeerContext`,`renderPeerTable`; `3684` bakes `peerScore[range]`; `12214` consumes for comp. NO peer-table baked for health/toxicity/age/response-rate chips. | `lib/peer-context.mjs:23-32` lists 8 supported metrics; only `comp` and `score` actually have data accessors (line 71-78). Other 6 return null. | medium (~2-3h) — implement remaining 6 metric accessors |
| C7. Cross-element graph nav — same company everywhere → same drawer | C | ⏳ partial | All-evaluations table: `2891` (data-drill="company:slug"); Drawer header: `12740-12752` (wires data-drill on drawer open); Banner ticker: `10171` (data-drill on company token); Apply-Now uses same renderRow logic. Heartbeat email: `scripts/heartbeat.mjs:329` uses `deeplink('company', d.company)` → `?focus=company:{slug}` BUT no `?focus=` handler in dashboard. Outreach Pulse `op-affil` (`scripts/build-dashboard.mjs:22285`) has NO data-drill. | Dashboard surfaces are wired. Heartbeat→dashboard deeplinks land on home page (no focus handler). Outreach pulse company names not clickable to company drawer. | small (≤30 min) for outreach pulse wire-up; small (≤30 min) for `?focus=` URL param handler |
| C8. Pre-emptive context in scan banners — enriched headline counts | C | ❌ not built | `scripts/build-dashboard.mjs:9442` (`<span id="live-text">—</span>`); `10293-10305` (`_liveFormat` builds `"Scanned {company} · {topRole} · {age}"` — no count rollup) | Banner shows latest single-company scan event. Doesn't aggregate "12 high-fit at ≥4.0, 5 at ≥$250K". | small (≤30 min) — add a second mc-strip chip aggregating apply-now-queue counts |
| C9. Decision provenance "why" trail one click away | C | ⏳ partial (drawer-only, not table-row-level for non-evaluated) | `scripts/build-dashboard.mjs:11506-11515` (drawer-why-disclosure pulls from `provenanceSummaries`); `2925` (tracker-note dcard drill); `3805-3818` (only baked for rows with `reportPath`) | Provenance exists in drawer but only for rows with eval reports. Status chip, comp chip etc don't surface their "why". | medium (~2h) — bake provenance for non-eval status changes (manual notes) |
| C10. Heartbeat → dashboard deeplinks `?focus=row:50` | C | ⏳ partial (heartbeat generates them; dashboard ignores them) | `scripts/heartbeat.mjs:888-889` (`deeplink(type,value)` → `?focus={type}:{value}`); used at `:1200,:1280,:329,:358,:396,:461`. Dashboard side: NO `params.get('focus')` handler exists in `scripts/build-dashboard.mjs` (grepped `params.get..'focus'` → 0 results). | Heartbeat links go to base dashboard; no auto-scroll, no auto-drawer-open. | small (≤30 min) — add focus-param handler that maps `row:N` → toggleDetail, `company:slug` → drillIn('company',slug), `apply-now` → scrollIntoView |
| C11. Wealth-lens click-to-estimate on every comp surface | C | ❌ not built | `scripts/build-dashboard.mjs:32` imports but `applyWealthLens` is never called. | (same as B3) | medium (~2h) |
| C12. Sticky comparative ribbon in drawers (← prev / next →, keyboard-nav) | C | ✅ verified | `scripts/build-dashboard.mjs:12762-12830` (`.prev-next-ribbon` w/ `_injectPrevNextRibbon`); `20811-20827` (CSS); `12841-12842` (`[` and `]` keyboard nav) AND a SECOND ribbon at `11886-11975` (`.drawer-ribbon-btn` w/ Alt+ArrowLeft/Right, `_populateDrawerRibbon`) | Two competing implementations present — both wired. Worth a dedupe pass eventually. | — |
| C13. Story child pages styled + footnotes + predicted Qs + prompts | C | ⏳ partial (architecturally complete; per-story content quality varies) | `lib/story-child-page.mjs:9-15` (sections: TLDR, Narrative, Predicted Questions, Remix Prompts, Footnotes); `468-480` (entry); generated files in `dashboard/stories/` (20+ files) | Surface confirmed. Content quality depends on `data/hm-intel/*` enrichment per role. | — |
| C14. Stories aligned with HM-intel research strategies | C | ⏳ partial | `lib/story-child-page.mjs:93-102` (`hm-intel/{companySlug}-{roleSlug}.json` lookup); `163-245` (HM-intel-aware prompt enrichment for predicted Qs); `223` (cover-letter remix gets HM-intel suffix) | Wiring shipped; quality depends on whether hm-intel JSON exists for the specific company+role. | — |
| C15. Pulse pipeline → drill-in deltas | C | ⏳ partial | `lib/company-pulse.mjs:6-28` (researcher dispatch + cache); `scripts/build-dashboard.mjs:12146-12151` (Social Signals section in company drill-in pulls from `cb.companyData[slug].pulseHtml`) | Pulse renders inline HTML when present; no delta-vs-prior-snapshot diff. | medium (~2h) — diff against last snapshot |
| C16. Popouts clickable inside (sweep done) | C | ✅ verified | All drill-in renderers use `event.stopPropagation()` on inner clicks (`12356`, `2891`, etc.) | — | — |
| C17. Ticker role + company both clickable → respective drawers; company profile depth | C | ✅ verified (depth: 9 sections, mostly placeholders pending data) | `scripts/build-dashboard.mjs:10171,10244` (clickability); `12042-12260` (9-section enhanced company profile: Header, Toxicity, Comp Range, Reviews, Social Signals, IPO/Funding, Funding Cycles, Active Roles, Network Leverage); each section either renders real data OR falls back to `_pendingCard` w/ a Refresh button | All 9 sections wired. Pending: many fall back to placeholder since underlying data files (`data/toxicity/{slug}.json`, `data/company-pulse/{slug}.json`, `data/company-funding/{slug}.json`) are sparse. | small (≤30 min) per section to populate the data files |
| C18. Tonight's Pick full popout flow (apply / learn-more / create / pick-another) | C | ✅ verified | `scripts/build-dashboard.mjs:9802-9805` (4 buttons: Start tonight's apply / Learn more / Generate apply pack / Pick another); `13054` (`tonightPickLearnMore`); `13283` (`tonightPickCycle`); `13153-13313` (`tonight-pick-create-btn` wiring) | All 4 actions wired | — |
| C19. TPgM cell relocated off home page | C | ✅ verified | `scripts/build-dashboard.mjs:3407,3866` (TPgM widget HTML stored for readiness drill-in, surfaced via sidebar readiness chip) | Moved to sidebar drill-in (matches the recent commit `e7ca884`) | — |
| C20. Truncation QA pass (cells + popouts) | C | ✅ verified | Confirmed shipped (P0-1, R2, R3 per prior commits) | — | — |
| C21. Role-drawer toolbar clickability + aesthetic | C | ✅ verified | `scripts/build-dashboard.mjs:11483-11495` (Apply / Generate apply pack / Skip / Look-at-this-later buttons w/ data-drawer-action + data-drill); P0-2 + P0-3 shipped | — | — |
| C22. Button + hyperlink contrast + visual cohesion | C | ✅ verified | P0-4 shipped (council consensus focus ring; CSS at `4070-4181`) | — | — |

---

## 1. Definitive Gaps That Could Ship Tonight (small effort, clear scope)

Priority order (highest value × lowest effort first):

1. **C10. `?focus=` URL param handler in dashboard** — Heartbeat is generating `?focus=row:N` and `?focus=company:slug` deeplinks at every important link, but the dashboard ignores them. ~20 lines of JS to parse the param on load and call `toggleDetail(rowNum)` or `drillIn('company', slug)`. Impact: every email click lands in the right drawer.
2. **C7. Outreach Pulse company → company drawer wiring** — Add `data-drill="company:{slug}"` to `op-affil` company text at `scripts/build-dashboard.mjs:22285` and a click handler. Cross-element graph nav becomes complete.
3. **C8. Pre-emptive context chips in scan banner** — Add a second mc-strip chip aggregating "{N} at ≥4.0 / {M} at ≥$250K" from `applyNow` filter logic that already exists at `scripts/build-dashboard.mjs:3150`. Pure read-from-baked-data work.
4. **C4. "Download as PDF" link on story child pages** — Wire `lib/pdf-child-page.mjs:wrapForPDFFlavor` into a `<a>` on every `dashboard/stories/*.html` page. The library exists; just needs a button.
5. **C2 quality fix. Bake `peerComp` for ALL rows with comp** — Currently `_cbData.peerComp` only populated for select rows. Loop through `apps.filter(r => r.notes?.includes('$'))` and bake `getPeerContext('comp', parseCompK(r.notes), {})` for each. ~10 lines.
6. **C17 data fill. Populate sparse company-data JSON files** — `data/toxicity/{slug}.json`, `data/company-funding/{slug}.json`, `data/company-pulse/{slug}.json`. Section UI already renders gracefully when data missing; just need the files. (Could be one slug at a time as part of normal eval work.)

## 2. Medium-Effort Items Requiring Multi-Step Work (defer with plan)

- **B3 + C11. Wealth-lens surfaced in comp drill-in.** Plan: at build time, call `await applyWealthLens(comp, role, {})` per row with comp data; bake the result HTML into `_cbData.wealthLens[drillId]`; in the comp drill-in renderer at `scripts/build-dashboard.mjs:12240`, insert the wealth-lens card BETWEEN peer table and equity sliders. Effort: ~2h including testing.
- **C6. Peer-context for remaining 6 metrics** (health, toxicity, age, response_rate, eval_to_apply_days, apply_to_response_days). Plan: extend `rowValue()` in `lib/peer-context.mjs:71` with accessors for each. Many require parsing the report markdown (eval date, apply date already in row.date / row.applyDate). Effort: ~2-3h.
- **C9. Decision provenance for non-eval status changes.** Plan: extend `lib/decision-provenance.mjs` to read manual status change history (from `data/status-changes.jsonl` if exists, else from row notes). Bake into `provenanceSummaries[num]` even when `reportPath` is null. Effort: ~2h.
- **B7. Travel-cap / comp-elasticity rule.** Plan: add `travel_pct` field to scoring schema in `modes/oferta.md`; new `lib/travel-cap.mjs` returning `{cap_breached, elasticated_acceptable_comp}` given base + travel%; surface in comp drill-in and score-rationale. Effort: ~2-3h including modes edit.
- **A4. Pipeline-pending + batch-runs sidebar entries.** Plan: add two `<a>` to sidebar at `9302-9323`; add `<section id="pipeline-pending-section">` + `<section id="batch-runs-section">` rendered from `data/pipeline.md` and `batch/batches-api-state.json`. Effort: ~2h.

## 3. Large / New Product Surfaces (not shippable tonight)

- **A3. ATS deep-apply form resolution.** Sketch: detect ATS platform from JD URL (Greenhouse / Lever / Ashby / Workday); call `lib/resolve-ats-url.mjs` (currently only used for in-page apply-button extraction, not the action-cell button) to resolve the form URL; fall back to JD on resolution failure. Risk: every ATS has a different form-URL pattern, breaks silently when the source page changes. Not worth tonight per spec.
- **A2 strict interpretation. 1-second cross-surface polling.** Sketch: WebSocket-based broadcast from `dashboard-server.mjs` to all open tabs on every JSONL append. The current event-driven model (`careerops:status-changed`) is arguably better than polling. Defer unless multi-tab sync becomes a real complaint.
- **C15 deep. Pulse pipeline diff deltas (Δ since last snapshot).** Sketch: write `data/company-pulse/{slug}.snapshot-{date}.json`; in the renderer, diff against the immediately-prior snapshot and highlight new themes / new posts. Useful but requires snapshot retention policy + UI design pass.

---

## Confirmed Shipped (no action needed)

All 9 dashboard table chip popovers (status, people, comp, equity, location, benefits, tier tag, gap modal, verify, email), pipeline modal + toast, Outreach Pulse banner + expand + reactivity, drawer 9-section HM intel, drawer prev/next ribbon + keyboard nav (DOUBLE-shipped — two implementations exist), drill-in registry with 17 types, universal `data-drill` attributes on every interactive element, Tonight's Pick 4-action popout, sticky scroll-to-Apply-Now KPI tile, sidebar readiness chip with TPgM drill-in, story child page architecture with HM-intel-aware predicted Qs + remix prompts, discard-with-reason loop closed end-to-end.

---

## Quick References

- Two ribbons present: `_injectPrevNextRibbon` at `scripts/build-dashboard.mjs:12775` + `_populateDrawerRibbon` at `:11886`. Both wired; dedupe is non-urgent.
- Wealth-lens lib is solid (`lib/wealth-lens.mjs:123,243` exports) but only `import`ed; zero call sites in dashboard.
- Peer-context lib has 8 metric types declared at `lib/peer-context.mjs:23-32`; only 2 have accessors at `:71-78`. The 6 missing accessors are the "partial coverage" gap on every non-comp/non-score chip.
- Heartbeat → dashboard handshake: heartbeat emits `?focus=row:N|company:slug|outreach|apply-now`; dashboard side has no consumer for these URL params.
