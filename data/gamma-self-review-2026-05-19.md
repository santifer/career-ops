# γ GAMMA Self-Review — Adversarial Audit 2026-05-19

Reviewer role: hostile adversarial reviewer. Task: break the fixes, not validate them.
Source: 9 commits between `4a04f4f` and `90f55b0`. All files read in post-fix state.

---

## Per-Fix Findings Table

| Fix | Finding | Severity | File:Line | What's Still Wrong |
|-----|---------|----------|-----------|-------------------|
| CRIT-1 | Two sibling strings in the same modal still say "runway calculation" after the fix claimed that lie was removed | HIGH | `scripts/build-dashboard.mjs:19571`, `:19692` | Line 19571: `"extends the runway calculation"` is inside `_rdExplainHealth('critical')` text visible on hover. Line 19692: `title="See runway calculation source"` is a tooltip on the Runway summary cell. Both contradict the fix's own assertion: *"Runway is an input, not a calculation."* The auditor doesn't scan for these text-string lies — it only checks `lib/*.mjs` file-existence. This is a live user-visible inconsistency. |
| CRIT-2 | `lib/strategy-ceiling.mjs` prompt variable is named `todayIso` — correct. But the fix comment says "3 LLM prompts" were patched; only 2 exist in `wealth-lens.mjs` (`todayIso` at :162, `todayIso2` at :348). The commit `3f9467a` message says it touched `lib/strategy-ceiling.mjs:119` plus those two. Verified: all three are dynamic. No bug. | PASS | — | This held up. All three date literals are live. |
| CRIT-3 | Pre-fix degraded cache entries **survive on disk and are actively served**. Two cache files exist right now (`52-interview_likelihood-xai-ai-sa-*.json`, `53-fit_score-perplexity-ai-sa-*.json`) created 28h ago with `_degraded: true` and fabricated `expected_lift_pct: 10`. `getCachedStrategy` has **no `_degraded` filter** — it checks `age > maxAgeMs` (24h default) and returns the blob. These files are 28h old and therefore expired by TTL, but only by 4 hours — any entry created within the last 24h will still serve fabricated numbers. The fix stopped WRITING new degraded entries to cache but left zero cleanup logic for existing ones. | CRITICAL | `lib/strategy-ceiling.mjs:78-90` | `getCachedStrategy` returns any cached blob that is < 24h old, `_degraded` or not. The renderer's `_degraded` check never fires on a cached entry because the flag comes from `computeStrategyCeiling`'s return path, not the cache-hit path: `return { ...cached, cache_key: cacheKey, _fromCache: true }` at :194 — `_degraded` IS in the cached blob (it was written by the pre-fix code path before the fix), so actually `_fromCache` is true AND `_degraded` is true. The renderer at line 375 does check `_degraded` and shows the amber warning. **But the lift badge suppression at :339 checks `expected_lift_pct == null`** — pre-fix entries have `expected_lift_pct: 10` (a number, not null), so the `+10%` badge renders anyway. The warning banner appears but the fabricated `+10%` chip also appears. Misleading compound state: user sees "unavailable" warning AND a numeric lift badge. |
| HIGH-1 | `scoreAlignment` with a **valid but wrong-format file** (exists, passes `existsSync`, but has no `**Score:**` header and no Block B) returns `{ unavailable: false, alignment: 0, interview: 12, hmNoticing: 2 }`. Three confident numbers, not `unavailable: true`. Tested live with `CLAUDE.md` as the report path: alignment=0, interview=12, hmNoticing=2. The dashboard draws three bars. `data_completeness.alignment` is `"fallback-to-score"` which the renderer uses to add a `⚠` chip — but alignment=0 from a score=0 parse failure is visually indistinguishable from a genuinely bad match. There is no floor or null check on `parseReportScore` returning 0 due to format mismatch. | HIGH | `lib/alignment-scorer.mjs:58-61`, `:103-112` | File-exists check is a proxy for "is a valid report." A malformed or wrong-file path silently produces three pseudo-confident bars. The `⚠` chip softens but does not prevent the misleading signal. |
| HIGH-1 | `scoreAlignmentCached` in-memory `Map()` has **no TTL and no invalidation**. A long-running `dashboard-server.mjs` caches alignment results for the process lifetime. If Mitchell edits a report (adding Block B data after initial import), the cached result never refreshes until process restart. | MEDIUM | `lib/alignment-scorer.mjs:271-280` | No `mtime` check, no TTL, no `clearCache` export. Unlike `lib/network-graph.mjs` which got the mtime fix (HIGH-3), alignment-scorer's cache was not touched. The audit inventory for HIGH-1 focused only on the missing-report case. |
| HIGH-2 | Confidence band (`high`/`med`/`low`/`very-low`) is computed in `rankCompaniesByWealth` and exported on each result object — but the **wealth-ranking drill-in renderer in `build-dashboard.mjs`** (~line 14906–14950) does not render it. The `partialBadge` chip appears when `r.hasPartialData` is truthy, but the confidence band (`r.confidence`) is never read by the renderer. Mitchell sees `partial data` (a generic badge) but never sees `very-low` confidence when 4 of 5 drivers are placeholders. The fix computed the band; the band was never wired to the UI. | HIGH | `scripts/build-dashboard.mjs:14906-14950` | `r.confidence` present in the data object, absent from the rendered HTML. |
| HIGH-2 | 5×5 default (5 missing drivers × 5 points each = 25 composite points) is still reachable. A company with **zero data** scores 25/100 from skill_portability default alone: 5 pts equity (unknown), 15 pts ai_native (inferred, no tier), 0 pts salary (no data), 0 pts IPO (no signals), 5 pts portability (median). Score = 25 with `confidence: very-low`. Because confidence is not rendered (finding above), the user sees a 25/100 score with only a `partial data` chip — no indication that 4 of 5 drivers are pure guesses. | MEDIUM | `lib/wealth-ranking.mjs:395-425` | The fix dropped one default (10→5 on skill_portability) but the composite floor for a zero-data company is still 20 points from ai_native + equity placeholders. |
| HIGH-3 | Race condition between `statSync` and `readFileSync` in `loadNetworkGraph`: `mtimeMs = statSync(GRAPH_PATH).mtimeMs` at :104, then `readFileSync(GRAPH_PATH, 'utf8')` at :107. If the file is deleted or replaced between these two calls, `readFileSync` throws. The `try/catch` at :119 swallows it and returns `null` — acceptable degraded behavior. But the `_cacheMtimeMs` is NOT updated on failure, so the stale `_cache` from the prior successful load persists alongside `_cacheMtimeMs` pointing at the old mtime. Next call: `statSync` gets the new mtime (or throws if deleted), comparison fails, and a re-read is attempted from a now-absent or partially-written file. This is a narrow TOCTOU window but a real one. | LOW | `lib/network-graph.mjs:103-121` | On race failure, `_cache` retains the old graph while `_cacheMtimeMs` retains the old mtime, creating a permanent stale-cache state until process restart or another mtime change. |
| HIGH-4/5/MED-1 | Toxicity source-quality weights (2.0 / 1.5 / 1.0 / 0.5) are **invented, not calibrated**. The commit message says they match "the same ranks used by dedupDrivers" — that is true (the dedup priority order is the same), but `dedupDrivers` itself is a tiebreaker rank, not a signal-quality calibration. A single council-of-models layoff finding (weight 2.0) produces `confidenceWeight = 2.0` → `med` confidence, not `high`. Two hm-intel matches (1.5 + 1.5 = 3.0) produce `high`. This means two noisy regex hits on narrative text outrank a single 7-LLM verified finding in the confidence band. The threshold `≥ 3.0 = high` was chosen so a single intel-cache hit gets `med` — but the commit message claims the fix was to give intel-cache *more* weight than regex hits. It does in the dedup priority, but the confidence-band math inverts this for the single-hit case. | MEDIUM | `lib/toxicity-composite.mjs:448-463` | No empirical calibration document. The 2.0/1.5/1.0/0.5 ladder and 3.0/1.5 thresholds are authorial choices presented as metric-truth. |
| HIGH-5 | `driversFromHMIntel` does NOT stamp `source_age_days` on its drivers (only `driversFromIntelCache` does). After dedup, an hm-intel-sourced driver wins over an applications.md driver (higher rank), but the winning driver has `source_age_days: undefined`. The aggregate `oldestDriverAgeDays` computation at line 479 filters `typeof a === 'number'`, so hm-intel drivers silently drop out of the freshness calculation. The UI sees `oldest_driver_age_days: null` when every driver is hm-intel-sourced, implying "freshness unknown" when the data might be months old. | MEDIUM | `lib/toxicity-composite.mjs:253-302` | `driversFromHMIntel` never calls `ageDaysFromMtime` — no `source_age_days` field on its outputs. MED-1 fix is half-wired. |
| Auditor | `checkFalseAttributions` only scans `scripts/build-dashboard.mjs`. False lib references in `dashboard-server.mjs` strings (e.g., tooltip text, error messages, comments with lib paths) are not caught. Tested: `dashboard-server.mjs` has string references to `lib/liveness.mjs`, `lib/decision-provenance.mjs`, `lib/diff-renderer.mjs` — all real, but if any became stale the auditor would not catch it. | MEDIUM | `scripts/agents/data-truth-auditor.mjs:118-153` | Audit scope is stated nowhere in the SKILL.md help text; a future agent running `--check-attribution` has no reason to know dashboard-server.mjs is excluded. |
| Auditor | `checkSilentZeroPatterns` heuristic looks for `return {` + `: 0` + `error|missing|unavailable` in 12-line windows. The `scoreAlignment` malformed-file bug (HIGH-1 above) returns `alignment: 0` alongside `data_completeness: { alignment: 'fallback-to-score' }`. The word `fallback` does not match `error|missing|unavailable` so this auditor sweep **does not catch the HIGH-1 malformed-file case**. | MEDIUM | `scripts/agents/data-truth-auditor.mjs:254-292` | The auditor cannot detect the specific failure mode it was supposed to prevent for HIGH-1. |
| Commit atomicity | Commit `21cb52d` bundles HIGH-4 (`lib/alignment-scorer.mjs` referral graduation) + HIGH-5 (`lib/toxicity-composite.mjs` confidence weighting) + MED-1 (source_age_days). These are three unrelated files with three unrelated semantic changes. Revert of `21cb52d` removes all three fixes or none — no clean surgical rollback path. | LOW | commit `21cb52d` | Mixed commit; noted as intentional in the message ("γ audit HIGH-4 + HIGH-5 + MED-1") but non-atomic for revert purposes. |

---

## Top 3 Most Actionable Issues to Fix Tonight

**1. CRIT-3 cache poisoning is live right now.**
`getCachedStrategy` must filter `_degraded: true` entries and re-trigger the LLM. One-line fix:
```js
// In getCachedStrategy, after age check:
if (raw._degraded) return null; // force re-attempt, do not serve fabricated numbers
```
Additionally delete all `data/strategy-cache/*.json` entries where `_degraded === true`.

**2. HIGH-2 confidence band is silently computed but never rendered.**
`rankCompaniesByWealth` returns `r.confidence` but the wealth-ranking drill-in HTML never reads it. Add a confidence chip alongside `partialBadge` in the renderer (line ~14929 in `build-dashboard.mjs`):
```js
var confBadge = r.confidence && r.confidence !== 'high'
  ? '<span ...>' + r.confidence + ' confidence</span>' : '';
```
Without this, the entire HIGH-2 fix is invisible to the user.

**3. CRIT-1 has two surviving "runway calculation" strings.**
`build-dashboard.mjs:19571` and `:19692` still say "runway calculation" — one is in the hover tooltip on the Runway summary cell (`title="See runway calculation source"`), one is in the critical-health explanation text. These are user-visible and directly contradict the fix's stated intent. Change both to "runway input" / "runway source."

---

## What I Tried to Break That Held Up

- **CRIT-2 date fix completeness.** I checked all three patched locations in `strategy-ceiling.mjs` and both locations in `wealth-lens.mjs` (`:162` and `:348`). All five use `new Date().toISOString().slice(0,10)` inside a template literal. The fix is complete and the auditor's `checkHardcodedDates` correctly passes on the post-fix state. The auditor's exclusion of comment lines and template-literal lines is correctly implemented.

- **HIGH-1 missing-report path.** When `reportPath` is `null` or points at a nonexistent file, `scoreAlignment` correctly returns `{ unavailable: true, alignment: null }`. The renderer at build-dashboard.mjs:2687 correctly checks `align.unavailable` before drawing bars. This specific path is clean.

- **HIGH-3 mtime-cache basic operation.** The `_cacheMtimeMs !== mtimeMs` check correctly triggers re-reads when the file changes. `clearCache()` export is present. The auditor's `checkFalseAttributions` correctly passes on the post-fix inventory.

- **HIGH-4 referral graduation math.** `computeHmNoticing` correctly falls back from `referralStrength` to `hasReferralPath` boolean without double-counting. The cache key in `scoreAlignmentCached` correctly includes `referralStrength` so a `none → direct` upgrade invalidates the stale entry.

- **Commit `10235db` (CRIT-1 runway tooltip) DOES fix the specific tooltip paragraph it targeted** — the `renderRunwayDetailModal` section at lines 19854–19862 now correctly says "Runway is an input, not a calculation." The two surviving lies are in SEPARATE functions (`_rdExplainHealth` and the summary cell renderer) that were not part of the original fix's scope.

---

*Generated by adversarial review session 2026-05-19. Word count: ~1500.*
