---
name: career-ops-tracker
description: Summarize and maintain the Career-Ops tracker. Use when the user asks about application status, next actions, tracker cleanup, canonical status updates, or wants a quick view of the current pipeline state.
---

# Career-Ops Tracker

1. Read `AGENTS.md` and `modes/tracker.md`.
2. Use `data/applications.md` as the canonical tracker.
3. You may update existing tracker rows, but do not add new rows directly.
4. For integrity checks and cleanup, use `verify-pipeline.mjs`, `normalize-statuses.mjs`, and `dedup-tracker.mjs`.
