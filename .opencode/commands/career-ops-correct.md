---
description: Manual outcome override for a report_id (positive | negative | neutral_excluded | inferred_negative)
---

Override the inferred outcome of an application manually. Usage:
`/career-ops-correct <report_id> <outcome> [reason]`

Load the career-ops skill:
```
skill({ name: "career-ops" })
```
