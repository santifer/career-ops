---
description: Batch PDF generation for multiple jobs in parallel
---

# Career Ops PDF2 Mode

Generate ATS-optimized CV PDFs for multiple jobs in parallel using career-ops pdf2 mode.

Load the career-ops skill:

```javascript
skill({ name: "career-ops", args: "pdf2" })
```
